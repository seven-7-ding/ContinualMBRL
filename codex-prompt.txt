帮我修改这里的代码：
【1】我需要实现的是两种新的reset方式：sandp，以及merge。每一种方式都分别有五种reset部分的选项：agent head, wm head, all head, only rssm, all。
【2】五种reset部位的选项分别对应了：
    1. agent head: 原先的only_agent_heads，但是要reset整个head，而不是最后的几层
    2. wm head: 原先的only_wm_heads，但是要reset整个head，而不是最后的几层
    3. all head：原先的all_heads，但是要reset整个head，而不是最后的几层，即所有的predictive heads
    4. only rssm: 原先的only_rssm，即整个dynamics模型
    5. all: 原先的reset_all，即整个系统
【3】sandp是shrink and perturb的缩写，即设置一个缩放比a。新的权重w设置为w=w*a+(1-a)*\epsilon，其中\epsilon是高斯噪声。
【4】merge方法，则设置一个缩放比a。新的权重w为w=w*a+(1-a)*w0.其中w0是整个系统在最一开始初始化时的权重值。这一措施即将现在的模型与最一开始的模型进行soft 融合。
【5】完成了上述的soft reset之后，还要将对应参数的optimizer进行reset，形式和原先的一致。
【6】添加revive机制（和原先的方法一致），但是默认设置revive的epoch为0（即不进行revive）。
【7】清除除了完成上述要求之外的所有无关文件和中间产物；原先代码中存在的与上述无关的代码也一并清洗一下，使得代码尽量简洁易懂。
【8】进行必要的debug和程序可行性实验（至少要看看能不能按照要求继续reset）。你可以在cuda3-7上进行实验，但是切记不要妨碍正在运行的进程（特别是，不要让程序占用的cuda内存太大，进而影响正在运行进程）。
【9】最后，在/home/jiale/MBRL/ContinualMBRL-soft-reset/auto_scripts/continual_mujoco_dreamer_priori_soft_reset.sh中进行修改，使得这个sh脚本
    1. 最大限度保持原先的执行逻辑
    2. 启动的进程中，让我可以**分别设置**reset的mechanism，以及reset的部位。当然，必要的其他超参数（包括frequency，task等等等等）也要让我可以自己设置。
    3. 要考虑的部位包括上述5种新的部位，以及no_reset。

---

【1】添加一个opt_only的reset_mechanism，即你上面提到的不进行参数值的reset，只进行optimizer的reset。
【2】并新添加以下的ablation 系列的reset target：
1. ab_encoder，除了encoder之外，全部reset
2. ab_rssm，除了dynamics之外，全部reset
3. ab_agent_head，除了agent_head之外，全部reset
4. ab_wm_head，除了wm的predictive heads之外，全部reset

完成以下任务：
【1】添加两种新的reset target：
    1. entire_agent: 包括encoder, dynamics, 以及agent的各个head
    2. entire_wm: 包括encoder，dynamics，以及world model的predictive head（reward，con，decoder等）
【2】写一份脚本用于检查/home/jiale/MBRL/ContinualMBRL-soft-reset/embodied/envs/general_dmc_priori.py中的dmc priori task的obs和act维度，并将找到的维度信息写入一份json文件放到/home/jiale/MBRL/ContinualMBRL-soft-reset/embodied/envs/文件夹中。
【3】帮我改写/home/jiale/MBRL/ContinualMBRL-soft-reset/auto_scripts/hard_task.sh脚本，
    1. 其中的task选用dog_walk，重复仅一次，task interval选用1e6。
    2. 对于该task，选用对应的obs dim/act dim。
    3. 使用sandp作为reset mechanism，reset target包括
        no_reset
        all
        ab_wm_head
        ab_agent_head
        agent_head
        wm_head
        entire_agent
        entire_wm
【4】检查【3】中启动实验时，如果obs dim和act dim与task不对应时，会发生什么？

在继续你当前的任务的同时，帮我添加和启动以下的实验：
【1】依次进行walker_stand|walk|run的实验，每个task 500k步，总共一轮。
【2】依次进行quadruped_run|dog_stand|humanoid_stand训练，每个task分别训练1M|2M|4M步，总共一轮。
【3】上述两组实验分别使用以下setting：
    "no_reset|1000"
    "no_reset|2000"
    "no_reset|3000"

    "all|1000"
    "all|2000"
    "all|3000"

    "ab_wm_head|1000"
    "ab_wm_head|2000"
    "ab_wm_head|3000"

    "ab_agent_head|1000"
    "ab_agent_head|2000"
    "ab_agent_head|3000"
    
    "agent_head|1000"
    "agent_head|2000"
    "agent_head|3000"

    "wm_head|1000"
    "wm_head|2000"
    "wm_head|3000"
【4】注意，你应该根据当前的RAM和cuda 显存状况进行依次的实验部署，并持续监控各个task的情况，确保各个实验成功运行完毕；且各个实验的训练fps不应该低于现在正在运行的task的80%。
【5】持续轮询和监视，时刻准备管理进程，同时时刻准备接收用户的新要求。不要自行结束和退出当前任务。

【1】将你之前实现的的shrink方法进行修改，修改后成为一个新的mechanism，名为shrink_skip_last。当选用这个reset mechanism时，reset target的最后一层输出层不参与reset （例如，value head的最后的mean layer和std layer）。请你确认这一特性是否在之前的shrink方法中也被满足，如果不满足则需要你按照上述要求修改代码，停下之前的shrink mechanism的三个run，改为跑新版本的shrink方法（其他设置都和之前的一模一样）；如果本来就满足那就不用再修改了。
【2】实现一个新的reset mechasnim，名为weight_scale。当指定完reset target后，和上面【1】的要求一样，对于最后的输出层不进行对应的操作。
    1. 在某个run一开始初始化后（仅在这个run的一开始），就对于每一层的[W|b]拼接成的矩阵计算其F范数，进行保存（记为f_0）。
    2. 在每个reset step （t），计算每一个需要被reset的layer的[W|b]构成的矩阵的F范数（f_t），并对于该层的W和b执行
        W' = W / f_t * f_0
        b' = b / f_t * f_0
        的操作，进行f范数的归一化。
    3. 这一个新的mechasnim，也根据/home/jiale/MBRL/ContinualMBRL-soft-reset/logdir/continual_dreamer_soft_reset_size1m中的设置，对于reset target=all进行3个seed的实验。其他的超参数等都不变。
【3】上述实验都需要优先跑上。现在cuda空间还较多，可以多放几个实验。适当调高replaybuffer的chunk size，只要不OOM即可。
【4】注意，还是要一直前台轮询，不要退出任务，直到用户给你明确指令允许退出。

codex resume 019e9132-588a-7973-ad4f-02aad8ab6aad