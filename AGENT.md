# Agent Context

- Preserve unrelated uncommitted changes in `/home/jiale/MBRL/MF-dreamer`; initial status showed auto script rename/delete state and `.gitignore` modification.
- Do not kill existing processes. Check RAM and CUDA load before launching training.
- Experiments must use conda env `dreamer`, wandb credentials from `.env`, project `continual_mfrl_size1m`, and run groups named by mechanism.
- Maintain `task_checklist.md` as requirements/progress only. Maintain `codex-cli-executor.log` with only recent 5 entries plus at most 1 important historical note.
- User allows deleting old code/scripts that are clearly not useful for the current SAC/DrQ continual task; prefer a clean repo over preserving stale task-specific launchers.
- Current safe launcher is `auto_scripts/sac_dreamer_dist_continual_mujoco.sh`; it queues runs until RAM, GPU free memory, and GPU utilization thresholds are satisfied. Default GPU utilization threshold is `GPU_MAX_UTIL=70`.
- The active deployment scheduler was restarted at 2026-08-11 16:38 HKT with `GPU_MAX_UTIL=60`; log path is stored in `logdir/scheduler/continual_mfrl_size1m_scheduler.logpath`, parent PID in `logdir/scheduler/continual_mfrl_size1m_scheduler.pid`.
- The priority scheduler is configured for `no_wsc_lr_default` and `wsc_skip_last_layer_dout_all`, seeds `1000/2000/3000`.
- As of 2026-08-11 16:42 HKT, `no_wsc_lr_default/seed_1000` is running with `grad_redo_enabled=True` and reached `walker_run` step 20000; the remaining five priority runs are queued under the same resource limits.
- SAC grad-ReDo is implemented as diagnostics inside the main SAC update, not as a parameter-reset path. It logs Dreamer-style `grad_redo/*` metrics without changing gradients or parameters, avoiding the earlier extra compile/reset path that hit ptxas 139.
- 2026-08-11 22:09 HKT: WSC slowdown was fixed by replacing Python/eager per-layer FNorm projection with cached per-prefix `jax.jit` projectors in `FlaxWSC`. Old slow WSC runs (`cfyqcsmz`, `xopoab3l`, `yzkyikr0`) were deleted from wandb/local logdir and relaunched. New WSC run ids observed: seed 1000 `b8ftggv8`, seed 2000 `a5745efg`, seed 3000 `siumuk78`.
- 2026-08-11 23:45 HKT: Remaining LR sweep runs were deployed with `JOB_SET=lr_sweep MAX_RUNS_PER_GPU=2`. Log path is stored in `logdir/scheduler/continual_mfrl_size1m_lr_sweep_scheduler.logpath`, PID in `logdir/scheduler/continual_mfrl_size1m_lr_sweep_scheduler.pid`. New groups: `no_wsc_lr_div10` and `no_wsc_lr_x10`, seeds `1000/2000/3000`.
- 2026-08-12 23:47 HKT: DrQ dmc-vision runs use project `continual_mfrl_size1m_dmc_vision`, tasks `walker_run,hopper_hop,cheetah_run`, `task_steps=1000000`, `task_repeats=5`, size1m, and conda env `dreamer`.
- Current dmc-vision experiments intentionally have `augmentation_enabled=False`; DrQ-v2 random shift remains implemented only as a future opt-in mechanism.
- 2026-08-13 HKT: The 12 DrQ dmc-vision MF-dreamer training runs were first OS-paused with `SIGSTOP`, then terminated by user request. Pause/stop records are in `logdir/scheduler/mfrl_pause_20260813_213007/`: `pause_manifest.json`, `pause_verify_clean.json`, and `stop_manifest.json`. The old `resume.sh` is disabled because these exact PIDs no longer exist; future continuation requires a fresh launch or a separately implemented disk-checkpoint restore.
- Stopped DrQ dmc-vision deployment had 12 Python training processes, 12 per-run `config.yaml` files, and JSON-safe local `metrics.jsonl` files under `logdir/continual_mfrl_size1m_dmc_vision`. At pause time all 48 related PIDs (12 train + 12 wrappers + 24 wandb children) were in state `T`; latest recorded steps were roughly 614k-648k depending on run.
- DMC vision launch excludes GPU0 and binds each run's `CUDA_VISIBLE_DEVICES`, `EGL_DEVICE_ID`, and `MUJOCO_EGL_DEVICE_ID` to the same physical GPU. Final active mapping: default seeds on GPUs 7/6/1; div10 seeds on GPUs 5/5/2; x10 seeds on GPUs 3/7/6; WSC seeds on GPUs 1/4/2.
- DrQ diagnostics now compute WB_FNorm/RMSNorm/LayerNorm/activation-rank stats on host and compute `grad_redo/*` from a separate diagnostics gradient tree, not from the main update JIT. Launcher uses the same compile lock for first update and diagnostics to reduce ptxas concurrency.
- `no_wsc_lr_div10/seed_2000` needed a retry with `XLA_FLAGS='--xla_gpu_force_compilation_parallelism=1 --xla_gpu_autotune_level=0'`; its current wandb run is `bn5vydy5`.
- 2026-08-13 22:41 HKT: Current active size12m deployment uses project `continual_mfrl_size12m_dmc_vision`, tasks `walker_run,hopper_hop,cheetah_run`, `task_steps=500000`, `task_repeats=5`, model size `size12m`, and groups `no_wsc`, `l2_init_2e-4`, `data_augmentation`, `WSC_skip_last_layer_dout_all` with seeds `1000/2000/3000`.
- Size12m monitor PID is recorded at `logdir/scheduler/continual_mfrl_size12m_dmc_vision_monitor.pid`; current PID observed as `3914397`, parented to init via `setsid`. It writes state to `logdir/scheduler/continual_mfrl_size12m_dmc_vision_monitor_state.json`, stdout to `logdir/scheduler/continual_mfrl_size12m_dmc_vision_monitor.stdout`, and safety/progress logs to `mfrl_vision_12m.log`.
- Size12m run launch mapping observed: `no_wsc` seeds 1000/2000/3000 on GPUs 6/1/2; `l2_init_2e-4` seeds 1000/2000/3000 on GPUs 6/4/5; `data_augmentation` seeds 1000/2000/3000 on GPUs 6/2/4; `WSC_skip_last_layer_dout_all` seeds 1000/2000/3000 on GPUs 7/1/3. GPU0 is not used for these new runs.
- Size12m monitor valid logging requires core SAC/DrQ loss/train keys, replay-buffer RAM metrics, Linear WB_FNorm, RMSNorm/LayerNorm output diagnostics, activation rank/dormancy metrics, grad/update diagnostics, and mechanism-specific metrics. As of 2026-08-13 22:49 HKT all 12 runs had at least 7 required valid local metric records and no error markers were found.
- W&B API check at 2026-08-13 22:52 HKT found 12 running runs in `stevendiffdiff-/continual_mfrl_size12m_dmc_vision`; all sampled summaries included replay-buffer RAM metrics and the full required diagnostic categories.
- Emergency policy for the size12m deployment: if system available RAM drops below 30000 MB, stop only the new size12m run process groups in reverse launch order and record it in `mfrl_vision_12m.log`. CUDA memory is monitored continuously; GPUs are 24GB class devices, so the 30GB redline is treated as system RAM rather than per-GPU VRAM.
- 2026-08-13 23:05 HKT WSC audit: `/home/jiale/MBRL/ContinualMBRL-wsc/embodied/jax/wsc.py` implements skip-last-layer by requiring a following RMSNorm, not by skipping only the last key. MF-dreamer's `FlaxWSC` already used this rule for `_SiLUMLP` `layer_i -> norm_i`, but DrQ vision `D4PGEncoder` Conv layers and `PixelMultiplexer` latent Dense used no RMSNorm or LayerNorm, so active pre-fix size12m WSC runs have `opt/wsc/controlled/*encoder_Conv_* = 0.0` and `*Dense_0 = 0.0`. Code is now patched so D4PG Conv uses `Conv_i -> RMSNorm(Conv_i_norm) -> SiLU`, PixelMultiplexer Dense uses `Dense_i -> RMSNorm(Dense_i_norm) -> tanh`, WSC following-norm matching supports `Conv_i_norm`/`Dense_i_norm`, and DrQ grad diagnostics skip only layers without following RMSNorm. The monitor was restarted as PID `3906814` and now marks current pre-fix WSC runs invalid under `opt/wsc/controlled/{encoder_Conv_0,Dense_0}=1.0`; the running size12m jobs were not stopped or restarted during this audit.
- 2026-08-13 23:40 HKT: Incorrect pre-fix size12m WSC runs were stopped and deleted from W&B/local logdirs. Deleted remote ids: `pmugzwd4`, `e7yy7ce4`, `tig7g82j`. Corrected WSC rerun ids: seed 1000 `yvuxlaro` on GPU7, seed 2000 `2d2p0z2f` on GPU1, seed 3000 `gmr0rolj` on GPU3. Each new run reached at least 3 required full logs; W&B summary confirms `actor/critic_encoder_Conv_0=1`, `actor/critic_Dense_0=1`, and actor/critic output heads remain `0`.
- 2026-08-14 22:59 HKT: Added `wsc_skip_last_layer_constantinit` to `FlaxWSC`. It keeps skip-last-layer semantics and uses per-layer target norm equal to the controlled layer's initial WB-FNorm divided by 8, instead of `sqrt(dout)/8`. Validation: `tests/test_wsc_skip_last_layer.py` passes and CPU DrQ smoke confirmed `target_norm = pre_norm / 8` on initialization.
- New `continual_mfrl_size12m_dmc_vision` constantinit runs are active: seed 1000 run `84a8lv6p` on GPU3 PID `4040076`, seed 2000 run `7z89thxl` on GPU5 PID `4040077`, seed 3000 run `8i4i80fb` on GPU7 PID `4040079`. Logdirs are under `logdir/continual_mfrl_size12m_dmc_vision/wsc_skip_last_layer_constantinit/seed_*`. Foreground polling confirmed each reached 5 valid logs by step 8000; W&B summary confirms WSC target/pre norms, controlled hidden Conv/Dense layers, skipped output heads, loss metrics, and replay RAM metrics.
- 2026-08-16 HKT: Do not resume a W&B run at an old/global step unless a matching model/optimizer/RNG checkpoint is restored. The 12 non-constantinit size12m runs were mistakenly resumed with fresh state and old W&B ids, causing replay size resets and score drops; those processes were killed and local bad logs were trimmed. W&B rewind is not enabled for this account/project, so remote bad points cannot be removed without deleting entire original runs. Future DrQ vision launches now support agent checkpoints and optional replay checkpoints.
